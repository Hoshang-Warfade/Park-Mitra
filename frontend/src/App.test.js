import { render } from '@testing-library/react';
import App from './App';

test('renders ParkMitra app', () => {
  render(<App />);
  // Basic smoke test to ensure app renders without crashing
  // Can add more specific tests later
});
